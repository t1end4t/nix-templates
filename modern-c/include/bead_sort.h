// include/bead_sort.h
#ifndef BEAD_SORT_H
#define BEAD_SORT_H

#include <stddef.h>

/**
 * Sorts an array of non-negative integers using the Bead Sort algorithm.
 *
 * @param arr The array to be sorted.
 * @param size The number of elements in the array.
 */
void bead_sort(int *arr, size_t size);

#endif /* BEAD_SORT_H */
